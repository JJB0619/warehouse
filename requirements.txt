# 核心构建工具（必须放在最顶部，优先执行）
setuptools>=75.0.0
wheel>=0.45.1
pip>=25.0.1

# 强制预编译安装pandas，彻底解决元数据生成失败
pandas==2.2.3 --only-binary :all:

# Flask 核心框架
Flask==3.0.3
Werkzeug==3.0.4
Jinja2==3.1.4

# 生产服务器（Railway必须用Gunicorn，替代Flask内置开发服务器）
gunicorn==21.2.0

# Excel 读写依赖（对应仓库系统导出功能）
openpyxl==3.1.5
xlrd==2.0.1

# 其他常用依赖（根据你的项目补充，以下为通用配置）
requests==2.32.3
python-dotenv==1.0.1